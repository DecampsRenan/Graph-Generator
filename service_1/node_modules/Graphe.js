// Classe utilitaire pour les graphes
var Graph = require('graph').Graph;

/**
 * Classe générant un objet graphe à partir
 * du fichier passé en paramètre.
 **/
var Graphe = function(nbNodes, density) {

	this.g = new Graph();

	var MAX_EDGES_PER_NODE = nbNodes - 1;

	var actuelNbOfNodes = 0;

	for(var i = 0; i < nbNodes; i++) {

		// On regarde si le sommet a déjà été construit
		if (this.g.degree('' + i)) {
			// Si oui, on regarde pour le lier à un autre sommet


		} else {
			// Sinon, on le construit


		}
	}

	console.log(this.g);
};

Graphe.prototype = {

	toString: function() {
		return this.g.toString();
	},

	toJSON: function() {

		return {
			nodes: [
				{ data: { id: 'a' } },
				{ data: { id: 'b' } },
				{ data: { id: 'c' } },
				{ data: { id: 'd' } },
				{ data: { id: 'e' } }
			],

			edges: [
				{ data: { id: 'ae', weight: 1, source: 'a', target: 'e' } },
				{ data: { id: 'ab', weight: 3, source: 'a', target: 'b' } },
				{ data: { id: 'be', weight: 4, source: 'b', target: 'e' } },
				{ data: { id: 'bc', weight: 5, source: 'b', target: 'c' } },
				{ data: { id: 'ce', weight: 6, source: 'c', target: 'e' } },
				{ data: { id: 'cd', weight: 2, source: 'c', target: 'd' } },
				{ data: { id: 'de', weight: 7, source: 'd', target: 'e' } }
			]
		};
	}
};

module.exports = Graphe;
